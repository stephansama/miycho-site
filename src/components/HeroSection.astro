---
import type { PageSectionsHeroSection } from "@tina-generated/types";
import { Image } from "astro:assets";
const {
	title,
	subtitle,
	social_links,
	background,
	//
} = Astro.props as PageSectionsHeroSection;
---

<Image
	src={background as string}
	alt=""
	width={100}
	height={100}
	class="size-[80%] mx-auto md:m-auto object-contain"
/>
